# Backend dependencies for 小说生成漫画应用
# Create a clean virtual environment before installing:
# python -m venv .venv && .venv\Scripts\activate

# Flask framework and related packages
Flask==2.2.5
Werkzeug==2.2.3
Flask-CORS==4.0.0
Flask-SocketIO==5.3.6

# Core dependencies
requests==2.31.0
python-docx==0.8.11
Pillow==9.5.0
pydantic

# Volcengine Ark SDK (image/LLM APIs)
volcengine-python-sdk

# Environment variables support
python-dotenv

# HTTP client
httpx>=0.24

# Crypto support
cryptography>=44.0.1