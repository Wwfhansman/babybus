# 小说生成漫画应用后端依赖 - 适用于Python 3.9.23

# Flask Web框架和相关组件
flask
flask-cors
flask-socketio

# SocketIO异步支持（推荐）
eventlet

# 文档处理
python-docx

# 火山引擎SDK - Python 3.9.23兼容 (官方推荐安装方式)
volcengine-python-sdk[ark]

# 开发和测试工具（可选）
# pytest==7.3.1
# pytest-cov==4.1.0

# 生产环境WSGI服务器（可选）
# gunicorn==20.1.0
# gevent==22.10.2

# 使用以下命令安装依赖：
pip install -r requirements.txt